/**
 * SuggestedPrompts Component
 * 
 * Displays a welcome message with suggested prompt area.
 * Currently minimal implementation - can be extended with actual prompt suggestions.
 */

export const SuggestedPrompts = () => (
    <div className="flex flex-col items-center justify-center h-full">
        <h2 className="text-xl font-semibold mb-6">What can I help with?</h2>
    </div>
);
